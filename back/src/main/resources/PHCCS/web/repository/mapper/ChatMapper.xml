<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="PHCCS.service.chatroom.repository.mapper.ChatMapper">

    <insert id="saveChatRoom" keyProperty="room.roomId">
        insert into chat_room
        values(#{room.roomId}, #{room.createMemberId}, #{room.participatingMemberId})
    </insert>

    <delete id="deleteRoom">
        delete from chat_room
        where room_id = #{roomId}
    </delete>

    <insert id = "saveChatLog">
        insert into chat_log
        (room_id, log, logger_id, write_time)
        values(#{roomId}, #{message.message}, #{loggerId}, #{message.timestamp})
    </insert>
</mapper>