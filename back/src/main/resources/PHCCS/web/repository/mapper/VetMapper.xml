<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="PHCCS.service.member.vet.VetMapper">

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into member(license_no, email, name,hospital_name, hospital_addr, request_date)
        values (#{licenseNo}, #{email}, #{name}, #{hospitalName}, #{hospitalAddr} #{requestDate})
    </insert>

</mapper>