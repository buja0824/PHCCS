<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="PHCCS.web.repository.mapper.PostMapper">

    <insert id="communitySave" keyProperty="post.id" useGeneratedKeys="true">
        insert into community_board (member_id, author, title, content, create_date, file_dir)
        values (#{post.memberId}, #{post.author}, #{post.title}, #{post.content}, #{post.writeTime}, #{post.fileDir})
    </insert>

    <insert id="qnaSave" keyProperty="post.id" useGeneratedKeys="true">
        insert into qna_board (member_id, author, title, content, create_date, file_dir)
        values (#{post.memberId}, #{post.author}, #{post.title}, #{post.content}, #{post.writeTime}, #{post.fileDir})
    </insert>

    <insert id="vetSave" keyProperty="post.id" useGeneratedKeys="true">
        insert into vet_board (member_id, author, title, content, create_date, file_dir)
        values (#{post.memberId}, #{post.author}, #{post.title}, #{post.content}, #{post.writeTime}, #{post.fileDir})
    </insert>

    <select id="showPost" resultType="Post">
        select id, author, title, content, view_cnt, create_date, file_dir
        from
        <choose>
            <when test= "category == 'qna_board'">
                qna_board
            </when>
            <when test= "category == 'community_board'">
                community_board
            </when>
            <when test= "category == 'vet_board'">
                vet_board
            </when>
        </choose>
        where id = #{id}

    </select>

    <select id="showAllPost" resultType="Post">
        select id, author, title, view_cnt, create_date as writeTime
        from
        <choose>
            <when test= "category == 'qna_board'">
                qna_board
            </when>
            <when test= "category == 'community_board'">
                community_board
            </when>
            <when test= "category == 'vet_board'">
                vet_board
            </when>
        </choose>
    </select>

    <update id="modifyPost">
        update
        <choose>
            <when test="param.category == 'qna_board'">
                qna_board
            </when>
            <when test="param.category == 'vet_board'">
                vet_board
            </when>
            <when test="param.category == 'community_board'">
                community_board
            </when>
        </choose>
        set title = #{param.title},
            content = #{param.content},
            update_date = #{param.modifyTime},
            file_dir = #{dir}
        where member_id = #{memberId} and id = #{postId}
    </update>

    <delete id="deletePost">
        delete from
        <choose>
            <when test= "category == 'qna_board'">
                qna_board
            </when>
            <when test= "category == 'community_board'">
                community_board
            </when>
            <when test= "category == 'vet_board'">
                vet_board
            </when>
        </choose>
        where member_id = #{memberId} and id = #{postId}
    </delete>

    <select id="findPostDir" resultType="String">
        select file_dir
        from
        <choose>
            <when test="category == 'qna_board'">
                qna_board
            </when>
            <when test="category == 'vet_board'">
                vet_board
            </when>
            <when test="category == 'community_board'">
                community_board
            </when>
        </choose>
        where id = #{id}
    </select>
</mapper>

