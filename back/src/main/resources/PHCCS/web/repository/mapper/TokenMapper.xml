<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="PHCCS.web.repository.mapper.TokenMapper">
    <insert id="insertRefreshToken">
            insert into refresh_tokens (id, subject, issuer, claims, issued_at, expiration, created_at)
            values (#{id}, #{subject}, #{issuer}, #{claims}, #{issuedAt}, #{expiration}, #{createdAt})
    </insert>
    
    <select id="getRefreshTokenById" resultType="PHCCS.domain.RefreshToken">
        select * from refresh_tokens where id = #{id}
    </select>

</mapper>