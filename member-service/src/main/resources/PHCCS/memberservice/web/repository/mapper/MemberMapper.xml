<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="PHCCS.memberservice.web.repository.mapper.MemberMapper">

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into pet(mb_no, mb_id, mb_password, mb_name, mb_nick, mb_addr, mb_pno)
        values (#{id}, #{email}, #{password}, #{name}, #{nickname}, #{address}, #{pnoNo})
    </insert>

</mapper>
